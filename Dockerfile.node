FROM ubuntu:22.04
RUN apt-get update && apt-get install -y \
bash curl openssl python3 argon2 \
socat postgresql-client jq \
&& rm -rf /var/lib/apt/lists/*
WORKDIR /app
COPY bin/ ./bin/
COPY lib/ ./lib/
RUN chmod +x bin/strongbox bin/strongbox-verify
CMD ["/app/bin/strongbox"]
